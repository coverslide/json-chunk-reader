{
  "name": "json-chunk-reader",
  "version": "0.1.1",
  "description": "A reader for newline-separated JSON strings",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "",
  "author": "",
  "license": "BSD",
  "dependencies": {
    "mkstream": "0.1.0",
    "jzon": "0.0.1"
  },
  "devDependencies": {
    "tape": "~1.0.2"
  }
}
